# Angular and scalar-series API review

Reviewed the frozen `/tmp/riesz-cap-checkpoint` versions of `AngularDistance.lean`, `SeparatedKernel.lean`, and `ScalarPowerSeries.lean`. The full root build was reported passing. I found no mathematical mismatch between the stated hypotheses, formulas, and intended endpoint use.

`AngularDistance.lean:6–31` defines squared chordal distance in colatitude coordinates and proves its exact half-angle decomposition. The comparison at lines 75–99 assumes both colatitudes lie in `[0,π]` and `|θ| ≤ π`, which are needed to keep the sine factors nonnegative and compare half-angle sine with angle. Lines 101–131 substitute `arccos` only for heights in `[-1,1]`, including the poles. The band estimates at lines 134–273 require `N ≥ 4` and `M ≥ 16`; the near case uses label gap at most two, and the separated case uses gap at least two. `band_radius_comparison` explicitly excludes the first and last cap bands, where a positive radius lower bound would fail at the poles.

`SeparatedKernel.lean:18–102` uses the polynomial ratio `4(1-s²)(1-t²)/(2-2st)²`. Under the stated separation inequality, `0 < 2-2st`, and `0 < ε < 1`, it proves `0 ≤ ratio ≤ (1-ε)² < 1`; the proof remains valid when either radius vanishes. It makes no ratio claim at zero axial distance, where the denominator vanishes. The factorization at lines 107–140 is purely polynomial with `m ≥ 2` and `a,b ≤ 2`, so it avoids negative powers of endpoint radii. Lines 143–175 establish polynomially weighted geometric summability for `0 ≤ q < 1`. These are algebraic and scalar convergence inputs; this module does not claim the full separated averaged-kernel derivative estimate.

`ScalarPowerSeries.lean:12–40` defines the function as the actual real infinite sum `∑' a_m z^m`. The bounded-coefficient hypothesis yields analytic regularity only on the open interval `(-1,1)` (lines 140–159); no endpoint differentiability or convergence is asserted. The derivative-coefficient recursion at lines 42–55 gives `(m+k).descFactorial k · a_(m+k)`, which is the usual shifted falling factorial. The radius bound for each differentiated series at lines 59–114 and the first-derivative identity at lines 116–138 support the all-finite-order formula at lines 162–200. In the induction, equality on the open interval supplies local equality before differentiating, so the formula is for ordinary `iteratedDeriv` at interior points. The fourth-derivative specialization is exactly the `k=4` case; it is not yet a bound on the two-variable separated kernel.

These statements preserve the polar endpoint and denominator assumptions needed for subsequent chain-rule work. I found no stronger claim than the proved API in these three modules.
