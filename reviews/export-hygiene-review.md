# Static review of the standalone exporter

**Verdict: pass for the simplified exporter as a static review.** The subsequent strict eight-theorem comparator also passed; see the [final verification record](../comparator/final-verification.md).

I reviewed `scripts/export_self_contained.py` and the source-only private-helper changes in `HarmonicL2Laplacian.lean` and `Longitude.lean`. The exporter traverses the root import DAG, removes import lines, and copies each remaining module body in dependency order without lexical rewriting of identifiers or proof text. It saves the standalone environment's `mainModule`, sets the original module identity before each source body, resets the module-local `auxLemmasExt` cache, wraps the body in `section ... end` to isolate `open`, variables, and local options, and restores the standalone identity after the final section. These commands address elaboration-state differences from merging files; they do not add axioms, change theorem types, or skip kernel checking.

Two bare private helpers, `restrictPolynomial_mul_apply` and `restrictPolynomial_add_apply`, occurred in both `HarmonicOrthogonality` and `HarmonicL2Laplacian`. The latter module now names them with an `_l2` suffix and updates their local uses. A separate visibility collision arose between the public `LatitudePotential.parallelPoint_dist_sq` and the private `Longitude.parallelPoint_dist_sq`; Longitude now calls its private helper `longitude_parallelPoint_dist_sq` and updates its sole use. These source edits leave the helper statements and proof steps unchanged. A scan of the current 105-source import traversal finds 239 bare private declarations, no duplicate private names, and no private names overlapping public declarations. Checking private names alone would have missed the Longitude collision. With these source renames, the exporter needs no global private-name substitution, which previously changed a proof expression in `finite_spectral_eval_cauchy_finset` despite successful standalone elaboration.

Earlier runs exposed the lexical-renaming mismatch and then the private/public collision. After those repairs, the [final comparator log](../comparator/verification-final.txt) records both source builds, exports of the same eight selected declarations, and successful kernel replay (`Solution valid. Your solution is okay!`). Its target list matches the [verification record](../comparator/final-verification.md), and all 106 entries in the [source hash manifest](../comparator/source-sha256.json)—105 modular sources plus the standalone source—match the current files. The static review alone did not certify theorem equality; the recorded full comparator PASS provides that separate check for the selected statements and their transitive definitions.
