# Cap lower-bound integration review

Reviewed `CapLowerBound.lean`, `BinomialTail.lean`, `SeparatedBinomialSeries.lean`, and their use in `Corollaries.lean` against the cap definition and Stolarsky normalization in `CapDiscrepancy.lean`. I found no mathematical target weakening or normalization error in this integration.

`CapLowerBound.lean:94–120` obtains the cardinality contribution from the diagonal of the **ordered** double sum for every indexed configuration; it never assumes injectivity. Lines 122–174 prove positive definiteness of each integer inner-product power by an explicit finite tensor-feature sum. Lines 237–372 show that the mixed empirical/surface terms equal the uniform term, so every power-energy gap is nonnegative, including odd degrees. For even degree `2r` with `r ≥ n`, lines 374–401 give the unnormalized gap at least `n/2` directly from the diagonal and the surface moment `1/(2r+1)`.

`BinomialTail.lean:8–40` defines the positive coefficients of `1 - sqrt(1-z)` and proves their recurrence and positivity. Its inverse-three-halves bound and dyadic block estimate at lines 124–180 yield `∑_{n≤r<2n} c_(2r) ≥ 1/(32√n)`. `SeparatedBinomialSeries.lean:46–96,146–190,202–286` supplies absolute convergence, termwise differentiation, and the binomial identity on `|z|<1`; this justifies the regularized kernel expansion used at `CapLowerBound.lean:404–426`. No endpoint series identity is silently assumed: `CapLowerBound.lean:718–849` first passes from finite partial sums to the interior regularized kernel and then sends `ρ ↑ 1` by bounded convergence to genuine chordal distance.

The final bridge is exact. `CapLowerBound.lean:851–881` identifies the uniform distance pair energy with `continuousEnergy 1` and the empirical one with `energy X 1`. Lines 884–916 use the proved Stolarsky identity with the manuscript's ordinary `dt`, hence `4 * capDiscrepancySq X = continuousEnergy 1 - energy X 1 / n²`; the derived lower bound is `1/(256 n√n)` for squared discrepancy. Lines 918–949 take the square root and prove `beckLowerBound` with constant `1/16` for all `n ≥ 1`. The `Function.Injective X` input in the existing `BeckLowerBound` interface is unused, so the result is stronger than the interface requires.

`Corollaries.lean:94–101` now closes the previously external cap lower-bound input through `beckLowerBound`, `constructionFacts`, and the proved `diamondStolarsky`. Its `cap_corollary_of_main` still assumes `MainTheorem 1`; it does not claim the upper discrepancy estimate or the paper's full theorem unconditionally. This status matches the remaining energy-proof boundary.

Verification: `lake env lean BEMOCFormalization/CapLowerBound.lean` exited successfully. This was a focused source review and targeted elaboration; the root agent is running the full frozen integration build.
