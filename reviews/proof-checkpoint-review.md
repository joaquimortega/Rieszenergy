# Mathematical alignment review of frozen checkpoint

Scope: `/tmp/riesz-proof-checkpoint` only, compared with its `definitive.tex`. This review does not cover later live working-tree modules or proofs. I found no concrete mismatch in the definitions and normalizations checked below.

The energy in `BEMOCFormalization/Core.lean:41` sums ordered pairs, matching `definitive.tex:121`; the diagonal vanishes for `0 < α`. The scale `N^(1-α/2)` at `Core.lean:45` and continuous constant `2^(α+1)/(α+2)` at `Core.lean:38` match the manuscript. `Construction.lean:7–43` uses `M = floor(sqrt(N/4))`, the manuscript's populations and midpoint bands, and independent phases. `constructionFacts` at `Construction.lean:563` proves the count and point distinctness for `N ≥ 4`. `sigma` at `Core.lean:34` is normalized mathlib spherical surface measure; `SurfaceMeasure.lean:15–22` proves total mass one, and `SurfaceMeasure.lean:1030` proves its uniform height marginal.

The cap observable at `CapDiscrepancy.lean:11,38–42,101–110` uses the manuscript's closed cap, empirical fraction minus normalized area, and squared integral against `dσ(u) dt` on `[-1,1]`. The height measure is ordinary `dt`, so the factor four in the stated `StolarskyIdentity` matches `definitive.tex:64–66,74–77`. The spectral observable at `Sobolev.lean:11–44` uses homogeneous harmonic polynomial restrictions, weights `(1+ℓ(ℓ+1))^s`, explicit summability, and the supremum of quadrature error over continuous functions. This matches the relevant norm and error in `definitive.tex:150–182`. `HarmonicBasis` at `Sobolev.lean:16–23` requires orthonormality and completeness; its existence is a separate, unproved checkpoint obligation.

Proof status is accurately represented by the checkpoint's documentation. `BEMOCFormalization.lean:12–17` explicitly calls this a scaffold and says `DefinitiveTargets` is a proposition describing the destination, not a proved theorem. In particular, `MainTheorem.lean:31` defines `MainTheoremTarget`, while the theorem at lines 34–36 still assumes `LatitudeBound α` and `LongitudeBound α`. `Corollaries.lean:19–27,60,87` proves conditional assembly implications; `DefinitiveTargets` at lines 30–33 is only a definition. `StolarskyIdentity` at `CapDiscrepancy.lean:109`, `BeckLowerBound` at line 401, and `SobolevEnergyComparison` at `Sobolev.lean:51` are stated obligations, not proved checkpoint results. The checkpoint also has no construction of `Nonempty HarmonicBasis` or proof of `SobolevOptimality` (`Sobolev.lean:62`). Accordingly, the full theorem and both corollaries from `definitive.tex:118–126,142–146,178–183` remain future work in this frozen snapshot. This is a completeness boundary explicitly disclosed by the project, not a semantic weakness or false result in the proved statements.
